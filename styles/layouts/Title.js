import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 48,
    fontWeight: "bold",
    color: "#FFFFFF",
    marginBottom: 32,
  },
});

export default styles;
